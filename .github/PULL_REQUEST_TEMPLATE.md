## What

<!-- One paragraph: the change and why now. -->

## Verification

<!-- Commands run and their results. A claim without its output is not
     verification; paste what you ran. -->

## Gates added or changed

<!-- For every NEW or MODIFIED CI step, guard test, check script, or
     assertion that something never happens: name the fault injected to prove the gate can
     fail — the mutation, the command, and the non-zero exit or failing
     assertion. A gate that has never failed has never been shown to exist.
     "None" is a valid entry for PRs that add no gate. -->
