name: "Publish to NPM"

# Co-releases the two published tooling packages under @hexagen-monaco at the
# tag's version (sync and arch-linter share a version). Each is a self-contained
# tsup bundle (ADR-0009 + amendment): private @hexagen/* deps are inlined, so the
# published manifests reference only third-party deps. Staged via
# prepare-publish-package.js, which strips workspace/private/dev fields and
# rewrites @hexagen/* → @hexagen-monaco/*. No @hexagen/shared publish and no
# propagation `sleep`s — the packages don't depend on each other at publish time.
on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    name: "Build & Publish @hexagen-monaco packages"
    runs-on: ubuntu-latest
    permissions:
      contents: write # softprops/action-gh-release creates a GitHub Release
      id-token: write # required for OIDC Trusted Publishing + provenance
    steps:
      - name: "Checkout Repository"
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      # Corepack must be enabled BEFORE setup-node: setup-node@v5's environment
      # probe runs `yarn` (e.g. `yarn cache dir`), and the repo pins yarn@4 via
      # packageManager — classic global yarn errors on that. Enabling Corepack
      # first makes `yarn` resolve to the pinned yarn@4 shim during the probe.
      - name: "Enable Corepack"
        run: corepack enable

      # No version literal: `corepack prepare --activate` with no spec reads
      # `packageManager` from the root package.json, so a Yarn bump is a
      # one-line change there and CI cannot drift from the declared pin.
      - name: "Prepare Yarn"
        run: corepack prepare --activate

      - name: "Setup Node.js"
        uses: actions/setup-node@v5
        with:
          # >= 22.9.0: npm 11 (Trusted Publishing) requires node ^20.17 || >=22.9.
          node-version: "22.12.0"
          registry-url: "https://registry.npmjs.org"

      # OIDC Trusted Publishing requires npm >= 11.5.1; Node 22.7 ships npm 10.x.
      # Pinned to a current stable (not @latest) so the release toolchain is
      # deterministic; bump deliberately rather than drifting on every npm release.
      - name: "Upgrade npm (Trusted Publishing support)"
        run: npm install -g npm@11.16.0

      # Fail fast (before the build) if the tag doesn't match what would be
      # published, and enforce the co-release invariant (both packages share a
      # version). GITHUB_REF_NAME is the tag, e.g. "v0.4.0".
      - name: "Verify tag matches package versions"
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          SYNC=$(node -p "require('./packages/sync/package.json').version")
          ARCH=$(node -p "require('./tools/arch-linter/package.json').version")
          if [[ "$SYNC" != "$ARCH" ]]; then
            echo "::error::Co-release version mismatch: sync=$SYNC, arch-linter=$ARCH"
            exit 1
          fi
          if [[ "$SYNC" != "$TAG" ]]; then
            echo "::error::Tag $GITHUB_REF_NAME (version $TAG) does not match package version $SYNC"
            exit 1
          fi
          echo "Tag and both package versions agree: $TAG"

      - name: "Install Dependencies"
        run: yarn install --immutable

      - name: "Build All Packages"
        run: yarn turbo run build

      - name: "TypeCheck All Packages"
        run: yarn turbo run typecheck

      # `typecheck` only covers each package's `src` — the base tsconfig
      # excludes __tests__. Run the test-source type gate before the suite so a
      # drifted fixture or mock is reported as a type error rather than as a
      # confusing downstream runtime failure. Same ordering as
      # sync-integrity.yml. Scoped to the publish closure (see next step).
      - name: "TypeCheck Test Sources (publish closure)"
        run: |
          yarn turbo run typecheck:test \
            --filter=@hexagen/sync... \
            --filter=@hexagen/arch-linter... \
            --continue --output-logs=full --log-order=stream

      # THE RELEASE TEST GATE.
      #
      # Until this step existed, publish.yml ran build + typecheck and then
      # pushed two packages to the public npm registry. No test ran anywhere on
      # the release path: sync-integrity.yml runs the suite, but only on
      # push/pull_request to main|develop — a `v*` tag triggers neither, and a
      # tag can be pushed at any commit. Generated customer projects consume the
      # `hexagen` binary from that registry.
      #
      # Scope is the *bundled* closure, not just the two root packages. The
      # staging script strips `workspace:` deps because tsup inlines
      # them into dist/ (ADR-0009, noExternal: [/^@hexagen\//]), so a tarball
      # physically contains @hexagen/{shared,governance,project-configuration,
      # template-engine,visualization} compiled in. Testing packages/sync alone
      # would leave most of the shipped bytes unexercised.
      #
      # The helper derives that closure from the manifests and cross-checks it
      # against the package set turbo actually resolves before running anything.
      # That check is not ceremony: `turbo run test --filter=<typo>...` exits 0
      # with "0 successful, 0 total", so a bare --filter step can silently
      # degrade into a green no-op. It also fails on a non-zero test exit — no
      # continue-on-error, no soft reporting. A red suite blocks the publish.
      #
      # NOT scoped to the whole monorepo on purpose: apps/web and the other
      # unpublished workspaces ship in no tarball, and gating a release on them
      # means unrelated failures block a publish.
      - name: "Test Published Closure (release gate)"
        run: node scripts/verify-publish-test-scope.js packages/sync tools/arch-linter

      # Stage self-contained, scope-rewritten manifests. Each `publish/` dir
      # holds dist/ + a public package.json (name → @hexagen-monaco/*, no
      # workspace:* deps).
      - name: "Stage @hexagen-monaco/sync"
        run: node scripts/prepare-publish-package.js packages/sync

      - name: "Stage @hexagen-monaco/arch-linter"
        run: node scripts/prepare-publish-package.js tools/arch-linter

      # Guard: a staged manifest must never reference the private monorepo scope
      # nor any unresolved workspace: protocol spec (which is uninstallable for
      # npm consumers).
      - name: "Verify staged manifests are consumer-installable"
        run: |
          for dir in packages/sync/publish tools/arch-linter/publish; do
            if grep -qE '@hexagen/|workspace:' "$dir/package.json"; then
              echo "::error::$dir/package.json references the private scope or a workspace: spec"
              cat "$dir/package.json"
              exit 1
            fi
          done

      # Pick the npm dist-tag from the version: a pre-release (e.g. 0.4.0-rc.1,
      # any version with a `-` identifier) publishes under `next` so it never
      # overwrites `latest`; a stable version publishes under `latest`. Both
      # packages share a version, so one tag covers the co-release.
      - name: "Resolve npm dist-tag"
        id: disttag
        run: |
          VERSION=$(node -p "require('./packages/sync/package.json').version")
          if [[ "$VERSION" == *-* ]]; then TAG=next; else TAG=latest; fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Publishing $VERSION under dist-tag '$TAG'"

      # No NODE_AUTH_TOKEN: auth is via OIDC Trusted Publishing. npm exchanges the
      # workflow's id-token for a short-lived publish credential, scoped to the
      # trusted publisher configured on each package (repo + this workflow). This
      # also produces provenance. Requires npm >= 11.5.1 (installed above).
      - name: "Publish @hexagen-monaco/sync"
        run: npm publish --provenance --access public --tag "${{ steps.disttag.outputs.tag }}"
        working-directory: packages/sync/publish

      - name: "Publish @hexagen-monaco/arch-linter"
        run: npm publish --provenance --access public --tag "${{ steps.disttag.outputs.tag }}"
        working-directory: tools/arch-linter/publish

      - name: "Create GitHub Release"
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
