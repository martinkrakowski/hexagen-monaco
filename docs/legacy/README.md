# Legacy Redirects

This directory contains pointer files for old documentation locations.

These files exist only to preserve external links and git history references. They point to the current canonical locations under the reorganized `docs/` structure (v2).

Do not add new content here.
