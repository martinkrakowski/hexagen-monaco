# Remaining-Work Consolidated Plan — 2026-07-25

**Context.** The entire PR backlog is merged (zero open PRs repo-wide; main @ `2f8a6484`, the Plan Workbench C2 squash). This plan consolidates everything that remains, grounded item-by-item against current code by a 13-agent verification pass (2026-07-25). Five previously-tracked items turned out to be **already shipped**; one **live production breakage** surfaced; the rest is organized into waves below.

**How to read this doc.** Every item carries a verdict from the grounding pass: `SHIPPED` (close it out, no code), `LIVE` (verified still broken/missing, scoped for implementation), or `DECISION` (blocked on a product call). File:line anchors were re-verified on `2f8a6484` — trust these over older plan docs whose line refs have drifted.

---

## 0. Urgent / operational (not PRs)

### 0.1 ⚠️ npm registry skew — prod scaffolds fail install (LIVE, severity: prod breakage)

`npm view @hexagen-monaco/sync version` = **0.8.0**, but the deployed engine stamps **0.8.1** (verified: `git show 76fa5bd1:packages/sync/package.json` = 0.8.1; 0.8.1 from #387 was never tag-published) and the repo is at **0.9.0** (#417, in the undeployed set). The F1 mechanism from the vellum arc (`resolveToolchainVersion`, `root-files.ts:115`) stamps the engine's own version into generated scaffolds — so every scaffold generated by prod today pins unpublished 0.8.1 and `yarn install` fails, exactly the original vellum F1 failure.

- **Action — both halves required, in this order:** (1) publish **v0.9.0** to npm (release-tag push is **Martin-gated**, live co-publish); (2) deploy main (§0.2), so prod stamps 0.9.0 — which then exists on the registry. Publishing alone does NOT fix prod (it would still stamp 0.8.1); deploying alone makes prod stamp 0.9.0, unpublished. Alternative if the deploy must wait: also publish 0.8.1 from the #387 tree — not recommended, just do both halves.
- **Hardening (Wave 4, vellum PR-2):** deploy-preflight step in `deploy.yml` that fails when the stamped sync version 404s on npm.

### 0.2 Deploy main to prod

Last deploy: 2026-07-12 at `76fa5bd1` (= the #411 squash itself; verified 2026-07-25 via `gh run list --workflow=deploy.yml` → success, and `git merge-base --is-ancestor`). Undeployed: exactly 14 commits — docs #412/#413, planning-layers #414–#416, **v0.9.0 bump #417 (load-bearing — it changes what scaffold version prod stamps, see §0.1)**, Plan Workbench #418–#425. Deploy is manual and Martin-gated: `gh workflow run deploy.yml --ref main` (build+SCP+compose, health-gated). Sequence AFTER the §0.1 npm publish.

### 0.3 Alvaro re-import validation

The #411 import fixes **are already live** (deployed 2026-07-12 — the deploy commit is #411's squash; same verification as §0.2). The re-import validation can run **now**, against current prod, independent of §0.2: re-import the alvaro project and diff findings against the original 25 (21 were pipeline-inflicted).

---

## 1. Already shipped — close-out actions only

| Item                                                                                       | Shipped by                                                                                                                                                                                                                                                              | Close-out action                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Issue #335 (.test.tsx in CI)                                                               | PR #367 (Vitest jsdom migration, 2026-06-25); `apps/web/vitest.config.ts:37` includes `**/*.test.tsx`, header comment cites #335; CI gates via `sync-integrity.yml:67` unfiltered `turbo run test`. Verified: 29 tsx files / 233 tests green inside the 872-test suite. | `gh issue close 335` with evidence comment; file XS follow-up issue for the two adjacent flags (`yarn workspace web test` → "command not found: vitest" bin-resolution gap, repo-wide decision; turbo test-task `inputs` globs are a landmine if test caching is ever enabled) |
| Issue #252 (hyphenated port names)                                                         | PR #318 deleted `wiring.ts`/`test-generator.ts` wholesale (D6 dead code, `c0970838`). Only surviving emit path sanitizes via `normalizeStubName` (`stubs.ts:157`).                                                                                                      | Close as not-planned with rebuild-guardrail comment; flip `generated-output-correctness-followups.md` A1 → OBSOLETE. **A2 (layer-folder resolver) and A3 (normalizer consolidation) remain independently valid** — routed to Wave 5                                            |
| `fix/project-import` branch (memory: 3 fixes applied, Stage 3 hang open)                   | Superseded by the import-hardening arc #407–#411; the branch no longer exists (verified `git branch -a`, 2026-07-25).                                                                                                                                                   | None — memory updated                                                                                                                                                                                                                                                          |
| Governance-chat follow-up "client fixer must exempt shared-kernel like server" (#399 note) | Already shipped: `manifest-violation-fixer.ts:136-139` returns shared-kernel contexts untouched ("type-only (R09) — legitimately own no ports").                                                                                                                        | None — memory updated                                                                                                                                                                                                                                                          |
| LLM PR-6 "6d infra"                                                                        | #339 (Alpine toolchain for better-sqlite3) + #340 (quota DB on named volume `hexagen-monaco-quota-data`), both 2026-06-14. Quota endpoint live; enforcement wired at 7 routes. No 6e exists.                                                                            | XS doc closeout (see 3.4) + Decision D2                                                                                                                                                                                                                                        |
| Nemotron gaps (context-name normalization, R08 assembler)                                  | #350 (normalization at all 4 prompt render sites) + #352 (`draftToManifest` emits `workspace:{name,description}`), both 2026-06-18, prod-validated.                                                                                                                     | None — but grounding found two NEW residuals, see 3.5                                                                                                                                                                                                                          |
| Scaffold phases 7/8                                                                        | Phase 7 = ADR-0041 (#271) + #272 (Applications step, `collapseApplications`/`fanOutApplications`); phase 8 = #268/#269/#270 (`--only` scope filter, protection-aware dry-run, idempotent no-op sync). The 8-phase plan is **complete**.                                 | Optional: status-table annotation in the plan doc                                                                                                                                                                                                                              |

These five can be batched into one hygiene pass (two `gh issue close` commands + one docs-only PR touching the three plan docs).

---

## 2. Quick wins — no decisions needed (§7 maps items to execution waves)

### 2.1 Issue #426 — LabeledInput label association (XS)

`LabeledInput.tsx:40-53` still renders unassociated `<label>`/`<input>` siblings; all 8 placeholder selectors live in exactly 4 suites. One PR, two halves that must land together:

- **Half 1:** destructure `id: idProp` explicitly (NOT left in `...rest` — if spread after a generated id, a caller id would override the input's id while `htmlFor` points at the generated one; comment this trap), `const id = idProp ?? useId()`, wire `htmlFor`/`id`. `ControlledLabeledInput` needs no change.
- **Half 2:** migrate all 8 selector sites to `screen.getByLabelText("Workspace Name"/"Namespace Prefix")` (keep the `as HTMLInputElement` casts — `.value` is read). Ambiguity verified safe: label text exists only in `IdentityFields.tsx`; suites render one settings section at a time.
- **Path corrections vs the issue text:** the seam suite is at `workspace-shell/__tests__/`, ProjectSettingsSection at `workspace-shell/plan-phase/__tests__/` — the issue's paths are wrong.
- **Gate:** `grep -rn 'placeholder="@mycompany"\|placeholder="@hexagen"' apps/web/features --include='*.test.tsx'` → zero hits. Closes #426.

### 2.2 Issue #258 — rescope, then classify-context-type timeout (S)

The `runPhase` half is **obsolete** — the stub was deleted by #331; every stage of the replacement pipeline already has per-attempt `AbortController` + tiered deadlines from `stage-timeout.ts`. The **live gap**: `classify-context-type.use-case.ts` has no internal timeout, and its only production caller (stage-2 low-confidence loop, `execute-structured-config-generation.use-case.ts:2154`) passes no signal → worst case N × ~60s silent stall.

- Comment on #258 citing #331 for the obsolete half; rescope to classify-context-type.
- Add internal `AbortController` + `setTimeout(STAGE_ATTEMPT_TIMEOUT_MS)`, composed with the optional external signal (copy the pattern at `execute-loose-spec-conversion.use-case.ts:88-122`). On deadline: `stageTimeoutError`, **no retry** (fail-fast contract; the caller already fail-softs to the heuristic type at `:2163-2170` — deliberately unchanged).
- Fake-timer tests mirroring `stage-timeout.test.ts` (use `vi.advanceTimersByTimeAsync`).

---

## 3. Small-to-medium PRs (sizes per item; §7 maps items to execution waves)

### 3.1 withCarriedName helper + crypto-comment sweep (S, one PR)

- **Helper:** new `apps/web/features/manifest-generation/carriedName.ts` — `withCarriedName(path, name)`: unchanged when name falsy, else append `name=${encodeURIComponent(name)}` with `?`/`&` chosen by existing query. Keep it **string-append** (URLSearchParams would reorder params and churn the name-legs suite). Replace the four builders: `GenerateWithAi.tsx:175-179` + `:226-229`, `ModelSelectionPage.tsx:61-66`, `ManifestAcceptPage.tsx:193-195/211/220` (name from `pendingManifest.projectName`, computed at render). Do **not** touch the read side (trim-vs-raw drift at `ModelSelectionPage.tsx:60` is round-trip-safe; unifying it changes URL echoes — separate decision). JSDoc must name the store-key contract (a dropped leg strands Section A edits).
- **Crypto comments:** `plan-phase-form-seam.integration.test.tsx:9-11` still carries the false "BEFORE the imports below" ordering claim (05dacc99 fixed only 3 of 4 suites); same false claim in different wording in `useProjectSettingsAutosave.test.ts:1-3` and `ProjectSettingsSection.test.tsx:1-2` — correct all three with the "Textual position is cosmetic: Vitest hoists imports…" wording.
- **Gates:** name-legs + workbench-integration + ManifestAcceptPage suites pass unmodified; `grep -rn 'BEFORE the imports below\|stubbed BEFORE that import' apps` → zero.

### 3.2 Shared ResizeObserver helper + steering gate at converged (S, one PR, two commits)

- **Helper:** the identical ~18-line stub block is now copy-pasted in **6** suites (was 4 at #421 review). New `apps/web/__tests__/support/resize-observer-stub.ts` exporting `installResizeObserverStub()` (registers beforeAll/afterAll internally, preserves restore-or-delete semantics). Location is deliberate: outside `features/` (no-cross-slice-imports rule, `eslint.config.js:37,42`), not globbed by vitest, and explicitly **not** `vitest.setup.ts` (locked #421 adjudication + CodeRabbit learning).
- **Steering gate:** `PlanPhaseView.tsx:359-366` maps every non-null non-done status to a live steering composer, but at converged/finalizing a note is provably never consumed (fold only feeds the NEXT model turn `fold.ts:47`; RESUME from converged is a reducer no-op `planning-session.ts:242-243`; distill reads only seed + last proposer turn `distill.ts:15`). The #421 deferral condition (C-phase defining resume semantics) is resolved. Export `canConsumeSteering(status)` from `planning-session.ts`, route `composerMode` through it, hide at converged/finalizing (mirrors done; right pane already shows the "Finalize to distill…" hint). Comment the never-consumed chain to pre-empt bots.
- **Decision D4 (soft):** hidden vs visible-but-disabled — plan assumes **hidden** (consistent with done); flag in PR for Martin.

### 3.3 Whole-array writers → record-level port methods (M) — ADR-0045 follow-up

All six writers still whole-array `saveProjects` on a possibly-stale snapshot (the pre-Phase-3 clobber class; `useSavedProjects` is instantiated per-consumer, so cross-instance staleness is real). Port fit found during grounding: `renameProject` + the three field-patch writers map 1:1 onto `updateProjectRecord`, but **`saveProject` and `deleteProject` need new port methods**:

- **Port widening** (`saved-projects-persistence.port.ts`): `createProjectRecord(project)` (fresh-read, duplicate-id reject, prepend) + `deleteProjectRecord(id)` (**idempotent** — absent id resolves success, or the hook's revert arm resurrects locally-deleted rows; document why this diverges from updateProjectRecord's NotFound contract).
- **Both adapters + FakePort in the same PR** (compile-enforced): IDB adapter (inside `enqueueWrite`), frozen-legacy localStorage adapter (composed load/save + "FROZEN legacy" comment), v4-migration FakePort.
- **Migrations:** `useSavedProjects.ts` saveProject→create (preserve await-durable-before-return-id — navigation gates on it), deleteProject→delete (keep fire-and-forget + seq revert), renameProject→update (NotFound = silent no-op, matching `updateProjectFormState:306-312`); `ExportContext.tsx:244-296` both persist helpers → `updateProjectRecord` (drop the pre-read; NotFound → warn+no-op); `useEditorPush.ts:79-94` same.
- **Tests:** adapter interleave pin (queued turn-append not reverted by create/delete/rename); ExportContext/useEditorPush persist helpers currently have **zero tests** — new focused tests must land with the migration. Amend ADR-0045's Negative bullet. `saveProjects` stays for migration steps only (annotate).

### 3.4 LLM plan-doc closeout (XS) + Decision D2

Truth up `llm-execution-and-free-tier-overhaul.md` (still says "PR-6 next"): all 7 PRs shipped (#305–#311, #334, #337–#340); as-built note (quota policy, better-sqlite3 at `/data/quota.db`, provider swap resolved as `LLM_MODEL` env = glm-5.2/OpenRouter, not mercury-2). Fix `deploy/.env.example:8` (claims LLM_MODEL is a variable; `deploy.yml:67` sources a secret). Encode D2's outcome (below); sqlite driver decision is de facto closed (keep better-sqlite3; revisit at node-22 base bump).

### 3.5 Stage-6 findings surface on /stage route (S) — new, found during grounding

The AI-prompt flow runs the full Stage-6 review then **throws the report away at the route boundary**: `stage/route.ts:165-172` emits done with only yaml/counts/transactionId while `result.state.stage6` carries the merged report; the /spec route surfaces it (`spec/route.ts:228-235`). Additive `validation: {errors, warnings, passed}` on the /stage done event; `useStagedManifestGeneration` reads it; AIGenerationPage renders with /spec-flow parity. Deliberately the B-lite slice — no navigation, store, or accept-gate changes (Part B of `stage6-async-review-and-context-name-fix.md` remains a separate, unshipped item). Optional XS companion: align deterministic R08 to `workspace.name/description` with system/scope fallback (keep the never-fires-post-assembly invariant). Named companion (scheduled in Wave 5, not just a PR-note): wire `synthesizeMissingRepositoryPorts` into `ExecuteFullStagedGenerationUseCase` (deferred from #354) — until then /stage can surface true R03s it can't self-heal, which becomes user-visible the moment this slice ships.

---

## 4. GitHub publish workflow-scope remediation (M, 2 stacked-by-deploy PRs)

Plan doc #413, zero code shipped; every claim re-verified live (auth scope still `read:user user:email repo` at `auth.ts:17`; every yarn-default OAuth scaffold publish still 404s).

- **PR-1 (server resilience, ship + deploy FIRST — restores publish for all users without re-auth):** `getTokenScopes` from `x-oauth-scopes` (null = unknown, fail-open — safe only because the reactive remap backstops it; comment this); proactive filter of `.github/workflows/*` + one warning per skipped file on new `ExportResult.warnings`; reactive createTree-404 remap keyed on the **local tree array** (GitHubApiError carries only status+message); writer preflight → new `workflow-scope-missing` error code; route mappings → 403 + `workflow_scope_required`. Design decision inside PR-1: the exporter error channel is a bare string collapsed at `generate-project-use-case.ts:202-207` — use a typed errorCode through the port (preferred) or a sentinel substring; keep parenthesized status codes out of the remap text (route regex `/\((401|403)\)/` collision).
- **PR-2 (after PR-1 is deployed):** auth scope + `workflow`; `fetch-json.ts` http-error gains optional `code`; ExportContext + UI surface `workflow_scope_required` (and wire the unused `reauth_required`) with a Reconnect GitHub action (`signIn("github")`).
- **Decisions D3 (Q1/Q2):** degrade-with-warning vs hard-fail; inline reconnect button vs copy — both have recommended defaults (degrade; inline button), work can start.
- **Post-deploy validation** needs a token verifiably lacking `workflow` scope (curl `x-oauth-scopes` preflight); then delete the two private debug repos and update the memory entry.
- **Explicitly deferred:** the doc's optional PR-3 (prefix raw GitHub error passthroughs with the failing operation name). **UX caveat for PR-2's copy:** existing sessions keep old tokens (jwt callback refreshes only on new sign-in), so the degrade path is _permanent_ behavior for un-reconnected users, not a transitional state — write the warning copy accordingly.

---

## 5. The two large arcs

### 5.1 Import round-trip integrity (L; doc #412, all four defect classes verified live)

Nothing shipped; one enabler landed incidentally (`updateProjectFormState` from #419 — exactly the variant PR-4 step 2 wanted, making it smaller than planned). Doc order holds; key drift notes from grounding: prompt file moved to `src/domain/prompts/`, `structuralManifestErrors` now exported at `:1728`, `usePendingManifest` gained layers-arc fields with a "set() doesn't touch originSession" contract that PR-3 **must preserve**.

1. **PR-1 (S) — judge R04/R05 discard+recompute** (never bare discard — the judge is the only default-path R04/R05 channel): same-context rewording in the prompt (`generate-manifest.prompt.ts:596-600`, mirror R06), extract `port-adapter-coverage.ts` (avoids the `:41` import cycle), add `04|05` to both discard regexes (`execute-validation-review.use-case.ts:283-284`) + deterministic per-context recompute gated on stage3+stage4, **preserving the shared-kernel exemption and empty-implements skip**.
2. **PR-2 (S–M) — stream silent death:** `sawTerminalFrame` in `useStagedGenerationStream.ts`; reader-end-without-terminal → failed + retry copy; error-frame branch in the residual flush (which must write React state); `timedOut` tag before the 300s watchdog cancel; surface the dead `retry()`.
3. **PR-3 (M) — fixer gate + hasFailures companion (same PR, mandatory — gating alone bricks approve):** `validationReport` into `usePendingManifest` (stream hook already exposes it; prompt flow needs `useStagedManifestGeneration` to re-expose — dovetails with item 3.5), skip the auto-fix loop when a report is present, `updateYaml` clears the report but not originSession, re-key hasFailures to Invalid-YAML + server `passed === false`, fix the parser `:225-232` first-match-wins overwrite.
4. **PR-4 (M) — manifestSource + export integrity:** `manifestSource: "imported"|"wizard"` + legacy discriminator in `normalizeLoadedProjects`; autosave guard routes imported projects through the already-shipped `updateProjectFormState`; exports send parsed manifest, **fail closed** on parse error (UX change — needs blocking-error copy; don't regress the wizard-authored live-first #222 path); read-only ports-step banner. **De-risking option:** the autosave guard (steps 1–2) is cherry-pickable as a standalone S-sized data-loss stopper if the full PR-4 slips.
5. **PR-5** stays unscoped (doc: "Phase A makes it non-urgent").

**Cross-arc guard:** shares `wizardToManifest` with the vellum arc — coordinate so projection fixes aren't double-applied (vellum touches emission defaults F2/F8/F15; round-trip touches parse/rebuild loss — different functions, same files).

### 5.2 Vellum generate-then-gate E2E harness + F-item batch (L)

~13 of 21 findings verified still live; zero remediation commits since #406. **Extend the existing capstone chassis** (`scripts/capstone/first-run-green.js` + `capstone.yml`) rather than building new — its fixture is a zero-context manifest and it never runs build/lint/typecheck/test, exactly the gap.

- **PR-1 (M):** `generate-then-gate.js` with two fixtures — (a) 15+-context modular monolith **built at runtime via `wizardToManifest()`** from a scripted answer set (byte-what-prod-emits; reproduces F2/F8/F15), (b) minimal + add-on templates (F9/F19/F3 surface). Gate steps in the generated tree: build, lint, typecheck, test, lint:arch, one non-turbo workspace lint (F8 detector), cross-package import probe (F5/F6/F7), `.env.*.example` staged-count assert (F3), orphan-file check (F9), `sync:check` zero-ops; actionlint + explicit corepack-before-setup-node and `package-manager-cache: false` assertions (F21). **Batch the fixes that make it green in the same PR:** F21 (S), F3 (XS), F2 (XS), F8 (S — `package-json.ts:30` reads root-level `workspaceDefaults` only while wizard-to-manifest nests under `monorepo:`; mirror tsconfig.ts's dual-location read), F15 (S), F4 (S). Risks: CI runtime 8–15 min for the big fixture — cache aggressively, consider main-only matrix; **F15's turboConfig consumption changes root `turbo.json` emission for EXISTING generated projects on their next sync — protected-file/idempotency semantics (`safeWriteFileAtomic` hash-skip) must be respected**.
- **PR-2 (XS + Martin gate):** the §0.1 npm publish + deploy-preflight.
- **Deferred (each becomes a red harness row until fixed):** F5/F6 packageResolution option (L), F9 workspace-aware templates (L), F10 type-only-context flag (M), F11–F13 sync ownership + `.tsx` in `isSourceFile` (M), F14 seeding-origin trace first (M), F16/F18/F19/F20 polish (XS–S).

---

## 6. Decisions needed from Martin

| #   | Decision                                                                                                                                                                                                                                                               | Options / recommendation                                                                                                                                                                                                                                                                                        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | **Publish v0.9.0 to npm + deploy main** (§0.1 — the two together fix the live prod scaffold breakage; publish alone leaves prod stamping unpublished 0.8.1)                                                                                                            | Go/no-go on tag push then deploy (both Martin-gated). Recommend: go, ASAP, publish-then-deploy                                                                                                                                                                                                                  |
| D2  | Free-tier double-charge: loose-spec import burns 2 of 10 daily generation credits (convert + spec); `/generate/local` also charges. Sub-decision: declare the PR-6 provider-swap goal met by `LLM_MODEL`=glm-5.2, or keep a mercury-2-for-chat env swap on the backlog | (a) unmeter convert (leaves only per-IP limiter on it) vs (b) keep + document intentional double-charge. Recommend (b) — zero code, no abuse surface change. Provider swap: recommend declaring met                                                                                                             |
| D3  | GH-publish Q1/Q2                                                                                                                                                                                                                                                       | Recommended defaults already in doc: degrade-with-warning; inline Reconnect button — work can start without a meeting                                                                                                                                                                                           |
| D4  | Steering composer at converged/finalizing                                                                                                                                                                                                                              | Hidden (recommended, mirrors done) vs visible-but-disabled                                                                                                                                                                                                                                                      |
| D5  | workspaceDescription reconciliation (amends locked plan §5 Q5; user edits are currently silently discarded at hand-off while the field stays editable)                                                                                                                 | (a) reconcile edited > AI-derived (+ `setManifestIdentity` grows optional description — allowed, top-level `description` is an existing YAML field); (b) hide/disable the Description field in genesis Section A; (c) status quo. No recommendation — genuine product call; (a) and (b) are both S once decided |
| D6  | Review-time defaults in 3.3 (rename NotFound arm; delete idempotency)                                                                                                                                                                                                  | Recommendations baked into the scope; veto at review if disagreed                                                                                                                                                                                                                                               |

---

## 7. Suggested execution order

```
Wave 0 (today):      D1 publish 0.9.0 → deploy main (§0.2) → alvaro re-import validation (§0.3, can start now)
Wave 1 (hygiene):    close #335/#252, rescope #258, docs closeout batch (§1, §3.4)  [1 docs PR + gh commands]
Wave 2 (parallel S): #426 · #258-timeout · carriedName+crypto · ResizeObserver+steering · /stage findings
Wave 3 (M):          array-writers (3.3) · gh-publish PR-1 → deploy → PR-2 (§4)
Wave 4 (L arcs):     round-trip PR-1→4 (§5.1) ∥ vellum harness PR-1 + batch, PR-2 (§5.2)
Wave 5 (gated):      D5 outcome · round-trip PR-5 · vellum deferred F-items · Stage-6 Part B
                     · synthesizeMissingRepositoryPorts into the full pipeline (companion to 3.5)
                     · generated-output-followups A2 (layer-folder resolver) + A3 (normalizer consolidation)
```

Wave 2 items touch **different hunks in three shared test suites** (`plan-phase-form-seam.integration.test.tsx` is touched by 2.1, 3.1 and 3.2; the workbench-integration suite by 2.1 and 3.2; `ProjectSettingsSection.test.tsx` by 2.1 and 3.1) — parallelizable, but expect rebases; **land #426 first** since its selector migration rewrites the helpers the others sit next to. The two Wave-4 arcs can run in parallel with the `wizardToManifest` coordination note from §5.1.

---

_Grounding evidence: 13-agent verification workflow `wf_ce241184-e74`, 2026-07-25, against main @ `2f8a6484`. Per-item file:line anchors in this doc were re-verified by that pass; older plan docs' line numbers have drifted (notably: staged-generation prompt moved to `src/domain/prompts/`; `useProjectLifecycle` churned ~25 lines by #419/#424/#425)._
