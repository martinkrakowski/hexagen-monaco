// `collapseApplications` / `fanOutApplications` and their types are no longer
// re-exported here: they are slice-neutral pure functions and now live in
// `lib/applications-config.ts`. Import them from `@/applications-config`.
export { ApplicationsStep } from "./ApplicationsStep";
