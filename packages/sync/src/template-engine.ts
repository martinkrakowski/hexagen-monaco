// Canonical implementation lives in @hexagen/shared.
// Re-exported here so existing generator imports (../template-engine) need no changes.
export type { InterpolationResult } from "@hexagen/shared";
export { interpolate } from "@hexagen/shared";
