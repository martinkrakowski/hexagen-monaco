/**
 * Re-export persistence types from @hexagen/shared for backward compatibility.
 * The canonical definitions now live in the shared kernel.
 */
export type {
  MonacoPersistencePort,
  SessionMetadata,
  PersistenceError,
} from "@hexagen/shared";
