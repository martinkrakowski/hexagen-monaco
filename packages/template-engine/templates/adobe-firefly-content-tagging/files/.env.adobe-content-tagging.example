# --- Adobe Firefly — Content Tagging ---
# Drop tags below this confidence (0-1). Install default sets the baseline;
# override per environment. Empty/invalid falls back to the install default.
ADOBE_TAGGING_MIN_CONFIDENCE={min_confidence}
