# --- Adobe Firefly — Generate ---
# Enabled operations: {operations}
# Model is env-overridable; default output size is set at install.
ADOBE_FIREFLY_DEFAULT_MODEL=firefly_v3
ADOBE_FIREFLY_SIZE={default_size}
