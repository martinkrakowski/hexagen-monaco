// Emitted by the google-oauth template — replaces auth-mock's throwing stub.
// auth-mock's src/infrastructure/auth/index.ts imports RealAuthAdapter from this path,
// so AUTH_MODE=real will instantiate GoogleAuthAdapter automatically.
export { GoogleAuthAdapter as RealAuthAdapter } from "../google/google-auth.adapter";
