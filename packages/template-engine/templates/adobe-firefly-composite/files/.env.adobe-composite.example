# --- Adobe Firefly — Composite ---
# Model is env-overridable; default candidate count is set at install.
# Empty/invalid values fall back to the install default.
ADOBE_FIREFLY_DEFAULT_MODEL=firefly_v3
ADOBE_COMPOSITE_CANDIDATES={default_candidates}
