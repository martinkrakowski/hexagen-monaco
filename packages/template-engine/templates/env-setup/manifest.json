{
  "id": "env-setup",
  "name": "Env Setup",
  "description": "Categorised .env.example, Zod runtime validation, check-env script, and SETUP.md first-day guide",
  "version": "1.0.0",
  "requires": [],
  "conflicts": [],
  "questions": [
    {
      "id": "framework",
      "type": "select",
      "prompt": "Server framework?",
      "options": ["next.js", "express", "fastify", "nitro"],
      "default": "next.js"
    },
    {
      "id": "strict_validation",
      "type": "boolean",
      "prompt": "Fail hard at startup if required env vars are missing?",
      "default": true
    }
  ],
  "envVars": ["NODE_ENV", "NEXT_PUBLIC_APP_URL"],
  "outputs": [
    ".env.example",
    ".gitignore.hexagen",
    "src/config/env.ts",
    "src/config/env.server.ts",
    "src/config/env.client.ts",
    "scripts/check-env.ts",
    "SETUP.md"
  ],
  "checklist": [
    "Install Zod: npm install zod",
    "Append the secret-ignore rules and remove the sidecar: cat .gitignore.hexagen >> .gitignore && rm .gitignore.hexagen (never commit real secrets)",
    "cp .env.example .env.local, then set every variable annotated # required (here and in any .env.<template>.example)",
    "Add a script to package.json: \"check:env\": \"tsx scripts/check-env.ts\"",
    "npm run check:env  ← validates .env.local before first run / demo / deploy",
    "Server code imports { serverEnv } from src/config/env.server; client code imports { clientEnv } from src/config/env.client (the src/config/env barrel re-exports types only)",
    "Read SETUP.md for the env model (server vs client split, strict validation)"
  ],
  "branch": "feature/generator-template-env-setup"
}
