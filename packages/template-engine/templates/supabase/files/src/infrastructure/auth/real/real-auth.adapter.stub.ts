// Emitted by the supabase template — replaces auth-mock's throwing stub.
// auth-mock's src/infrastructure/auth/index.ts imports RealAuthAdapter from
// this path, so AUTH_MODE=real will instantiate SupabaseAuthAdapter automatically.
export { SupabaseAuthAdapter as RealAuthAdapter } from "../supabase/supabase-auth.adapter";
