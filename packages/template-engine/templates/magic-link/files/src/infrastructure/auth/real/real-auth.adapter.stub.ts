// Emitted by the magic-link template — replaces auth-mock's throwing stub.
// auth-mock's src/infrastructure/auth/index.ts imports RealAuthAdapter from this path,
// so AUTH_MODE=real will instantiate MagicLinkAuthAdapter automatically.
export { MagicLinkAuthAdapter as RealAuthAdapter } from "../magic-link/magic-link-auth.adapter";
