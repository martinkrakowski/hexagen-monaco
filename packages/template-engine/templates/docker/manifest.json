{
  "id": "docker",
  "name": "Docker",
  "description": "Multi-stage Dockerfile, docker-compose with peer services, dev override for hot reload, GitHub Actions image push",
  "version": "1.0.0",
  "requires": [],
  "conflicts": [],
  "questions": [
    {
      "id": "node_version",
      "type": "select",
      "prompt": "Node.js version?",
      "options": ["20", "22", "23"],
      "default": "22"
    },
    {
      "id": "services",
      "type": "multiselect",
      "prompt": "Additional services in docker-compose?",
      "options": ["redis", "postgres", "mailhog", "minio"],
      "default": []
    },
    {
      "id": "registry",
      "type": "select",
      "prompt": "Container registry?",
      "options": ["ghcr", "ecr", "docker-hub", "none"],
      "default": "ghcr"
    },
    {
      "id": "health_check",
      "type": "boolean",
      "prompt": "Include health check in Dockerfile?",
      "default": true
    }
  ],
  "envVars": [],
  "outputs": [
    "Dockerfile",
    ".dockerignore",
    "docker-compose.yml",
    "docker-compose.override.yml",
    "docker-compose.ci.yml",
    ".github/workflows/docker-build.yml"
  ],
  "checklist": [
    "Create .env.local (docker-compose reads it via env_file) — copy from .env.example",
    "docker compose up --build   (dev mode + hot reload; merges docker-compose.override.yml automatically)",
    "Enable backing services with profiles, e.g. COMPOSE_PROFILES=redis,postgres docker compose up — or persist COMPOSE_PROFILES in .env.local",
    "Verify the app at http://localhost:3000/api/health (health route comes from the observability template)",
    "Production-equivalent run: docker compose -f docker-compose.yml -f docker-compose.ci.yml up --build",
    "ghcr.io needs no extra secrets (uses GITHUB_TOKEN). For Docker Hub/ECR, set the REGISTRY_ORG repo variable and switch the commented login step in .github/workflows/docker-build.yml"
  ],
  "branch": "feature/generator-template-docker"
}
