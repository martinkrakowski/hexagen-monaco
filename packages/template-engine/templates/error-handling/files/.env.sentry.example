# -------- Sentry (error tracking) --------
# Get these from https://sentry.io → Settings → Projects → <project> → Client Keys.
# Merge into .env.local. The error handler reports unhandled errors when
# @sentry/node is installed.
SENTRY_DSN=          # required
SENTRY_ORG=
SENTRY_PROJECT=
