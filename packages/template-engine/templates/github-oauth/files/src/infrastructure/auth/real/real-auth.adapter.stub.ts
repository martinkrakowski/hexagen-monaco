// Emitted by the github-oauth template — replaces auth-mock's throwing stub.
// auth-mock's src/infrastructure/auth/index.ts imports RealAuthAdapter from this path,
// so AUTH_MODE=real will instantiate GitHubAuthAdapter automatically.
export { GitHubAuthAdapter as RealAuthAdapter } from "../github/github-auth.adapter";
